# Sample Data Workflow

1. Copy sample screenshots into `project/data/uploads/<session_id>/`.
2. Use frontend upload to start a fresh session.
3. Run analysis and review JSON output.
4. Export DOCX/PDF/Markdown from UI.
